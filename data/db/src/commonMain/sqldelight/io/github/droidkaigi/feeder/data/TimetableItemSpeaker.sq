CREATE TABLE timetableItemSpeaker (
    timetablePrimaryId TEXT NOT NULL,
    name TEXT NOT NULL,
    iconUrl TEXT NOT NULL,
    bio TEXT NOT NULL,
    tagLine TEXT NOT NULL
);

insert:
INSERT INTO timetableItemSpeaker(
    timetablePrimaryId,
    name,
    iconUrl,
    bio,
    tagLine
) VALUES ?;

deleteAll:
DELETE FROM timetableItemSpeaker;
